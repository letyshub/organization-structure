services:
  db:
    image: postgres:15-alpine
    restart: always
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: org_structure
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    ports:
      - "4821:4821"
    environment:
      DATABASE_URL: postgresql://user:password@db:5432/org_structure?schema=public
      JWT_SECRET: supersecret
      PORT: 4821
    depends_on:
      - db
    volumes:
      - ./api/uploads:/app/uploads

  web:
    build:
      context: ./web
      dockerfile: Dockerfile
    ports:
      - "3914:3000"
    environment:
      REACT_APP_API_URL: http://localhost:4821
      WDS_SOCKET_PORT: 3914
    depends_on:
      - api

volumes:
  postgres_data:
